class Square {
  name: string;
  color: string;
  url: string;

  constructor(name: string, color: string, url: string) {
    this.name = name;
    this.color = color;
    this.url = url;
  }
}

export default Square;
